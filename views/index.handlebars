<div class="jumbotron" id="area">
    <h1 class="display-4">Bem-vindo ao Blog</h1>
    <p class="lead">Este é um Blog desenvolvido com Node JS + Express + Mongo DB</p>
    <hr class="my-4">
    <p>Aqui você vai poder ler sobre as novidades do mundo da computação.</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
    </p>
</div>
<hr>
<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-5">
    <div class="card-body">      
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Publicado: {{data}}</small>   
    </div>       
</div>
{{else}}
    <h5>Nenhuma postagem registrada</h5>
{{/each}}